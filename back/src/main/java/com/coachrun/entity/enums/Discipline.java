package com.coachrun.entity.enums;

/**
 * Discipline d'un athlète (cf. DARI Lab). Conditionne l'affichage (Route = teal, Trail = orange)
 * et le regroupement des athlètes sur le dashboard coach.
 */
public enum Discipline {
    ROUTE,
    TRAIL
}
