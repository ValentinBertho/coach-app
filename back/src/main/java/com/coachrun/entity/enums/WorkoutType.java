package com.coachrun.entity.enums;

/** Type de séance prescrite (cf. Claude.md §2). */
public enum WorkoutType {
    ENDURANCE,
    RECOVERY,
    TEMPO,
    THRESHOLD,
    INTERVALS,
    LONG_RUN,
    RACE,
    STRENGTH,
    CROSS_TRAINING,
    REST
}
