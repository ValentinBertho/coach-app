package com.coachrun.entity.enums;

/** Portée d'une zone d'entraînement : définie au niveau du club ou propre à un coach. */
public enum ZoneScope {
    /** Zone partagée par tout le club. */
    CLUB,
    /** Zone propre à un coach (personnalisation). */
    COACH
}
