package com.coachrun.entity.enums;

/** Type de série au sein d'un exercice de force (cf. DARI Lab). */
public enum SetType {
    STANDARD,
    DROP_SET,
    SUPER_SET,
    MYO_REPS,
    CLUSTER,
    ISO_OVERCOMING,
    ISO_YIELDING
}
